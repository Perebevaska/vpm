"""wt — свой WireTurn-совместимый сервер поверх WebDAV.

Слои (снизу вверх):
  crypto   — вывод ключа + AES-256-GCM для чанков
  webdav   — синхронный WebDAV-клиент (requests)
  pipe     — упорядоченный дуплексный байт-канал поверх чанк-файлов
  yamux    — мультиплексор потоков (своя реализация wire-протокола)
  socks    — upstream SOCKS5-клиент (egress в цепочку)
  server   — поллинг сессий + accept + релей
"""
